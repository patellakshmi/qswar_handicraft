server:
  port: 9091

spring:
  profiles:
    active: dev
  application:
    name: handicrafts
  datasource:
    url: jdbc:mysql://localhost:3306/hc?useSSL=false&serverTimezone=UTC
    username: root
    password: root@123
    driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
      hibernate:
        ddl-auto: validate
      show-sql: true

  session:
    store-type: redis
    redis:
      host: localhost
      port: 6379

